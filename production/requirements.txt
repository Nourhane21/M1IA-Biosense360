# BioSense360 — Dépendances du module production
# ================================================
# Généré pour Python 3.9+
# Installer avec : pip install -r requirements.txt

# --- API REST ---
flask>=2.3.0,<4.0.0

# --- Modèle IA ---
scikit-learn>=1.3.0,<2.0.0
joblib>=1.3.0

# --- Manipulation de données ---
pandas>=2.0.0,<3.0.0
numpy>=1.24.0,<3.0.0

# --- Tests ---
pytest>=7.4.0
pytest-cov>=4.1.0       # Couverture de code

# --- Optionnel : déploiement production ---
# gunicorn>=21.0.0       # Serveur WSGI pour la prod (Linux uniquement)
# waitress>=3.0.0        # Serveur WSGI cross-platform (Windows compatible)
